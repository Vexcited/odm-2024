import HeroLanding from "~/components/landing/hero";
import SearchNewlyCreated from "~/components/search/newly-created";

export default function HomePage () {
  return (
    <main>
      <HeroLanding />
    </main>
  );
}
